<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.animaltraining">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".GalleryImage.GalleryImageActivity" />
        <activity android:name=".GalleryImage.ShowImageInfoActivity" />
        <activity android:name="com.example.asus.myproject.ListNumber.ListNumberActivity" />
        <activity android:name="com.example.asus.myproject.ListNumber.CallActivity" />
        <activity android:name=".GalleryImage.HomePageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ColorsActivity"></activity>
    </application>

</manifest>